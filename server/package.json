{
  "name": "xbase-server",
  "version": "1.0.0",
  "description": "XBase backend: booking requests, cafe menu and club prices stored in SQLite, served over a REST API.",
  "main": "src/index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "seed": "node src/seed.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
